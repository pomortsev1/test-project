@AGENTS.md

Always follow the completion rules in `AGENTS.md`.
Call out every required follow-up step after changes land, including app server restarts, database update commands, dependency installs, or any other manual commands.
Provide the exact command for each required step.
